/**
 * GraphQL exports
 *
 * All types and hooks are generated from the backend API schema via codegen.
 * Run `pnpm codegen` to regenerate after API changes.
 *
 * Usage:
 *   import { useGetWarehousesQuery, Warehouse } from '@/graphql';
 */

export * from '../generated/graphql';
